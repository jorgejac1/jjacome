import { getPortfolio, type Locale } from "@/lib/i18n";
export default function Experience({ locale }: { locale: Locale }) {
 const portfolio = getPortfolio(locale); const t = (en: string, es: string) => locale === "es" ? es : en;
    return <main id="main" className="shell" data-content-version={portfolio.version}><header className="page-heading"><p className="eyebrow">{t("Career / Experience", "Trayectoria / Experiencia")}</p><h1>{t("Hands-on engineering.", "Ingeniería en la práctica.")}<br />{t("Shared direction.", "Dirección compartida.")}</h1><p>{portfolio.identity.summary}</p><div className="actions"><a className="button" href={portfolio.links.resume} download>{t("Download r\u00e9sum\u00e9 \u2193", "Descargar CV ↓")}</a></div></header>{portfolio.roles.map(role => <section className="role" key={role.id} aria-labelledby={`role-${role.id}`}><div><h2 id={`role-${role.id}`}>{role.company}</h2></div><div>{role.positions.map(position => <div className="position" key={position.title}><h3>{position.title}</h3><p className="period">{position.period}</p><ul>{position.highlights.map(item => <li key={item}>{item}</li>)}</ul><div className="tag-list">{position.technologies.map(tech => <span key={tech}>{tech}</span>)}</div></div>)}{role.id === "amex" ? <p className="evidence">{t("Component, mentoring and performance figures are historical r\u00e9sum\u00e9 claims for American Express. The load-time measurement has not been reproduced here.", "Las cifras de componentes, mentoría y rendimiento corresponden al historial de American Express documentado en el CV. La medición del tiempo de carga no se ha reproducido aquí.")}</p> : null}</div></section>)}<section className="role"><h2>{t("Education", "Formación")}</h2><div><h3>{portfolio.education.degree}</h3><p className="period">{portfolio.education.institution} · {portfolio.education.period}</p></div></section><section className="role"><h2>{t("Recognition", "Reconocimientos")}</h2><div>{portfolio.awards.map(award => <div key={award.title}><h3>{award.title}</h3><p className="period">{award.organization} · {award.date}</p><p>{award.description}</p></div>)}</div></section><section className="role"><h2>{t("Languages", "Idiomas")}</h2><p>{portfolio.languages.join(" · ")}</p></section><section className="contact"><div><h2>{t("Let\u2019s build something useful.", "Construyamos algo útil.")}</h2><a className="text-link" href={portfolio.links.email}>{t("Contact Jorge \u2197", "Contactar a Jorge ↗")}</a></div></section></main>;
}

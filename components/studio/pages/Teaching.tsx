import Link from "next/link";
import { getPortfolio, localePath, type Locale } from "@/lib/i18n";
export default function Teaching({ locale }: { locale: Locale }) {
 const portfolio = getPortfolio(locale); const t = (en: string, es: string) => locale === "es" ? es : en;
    const { teaching } = portfolio;
    return <main id="main" className="shell" data-content-version={portfolio.version}><header className="page-heading"><Link className="text-link" href={localePath(locale, "/")}>{t("\u2190 Back to portfolio", "← Volver al portafolio")}</Link><p className="eyebrow" style={{ marginTop: 30 }}>{t("Engineering note / Public teaching example", "Nota técnica / Ejercicio público")}</p><h1>{teaching.exercise.title}</h1><p>{t("A small exercise in separating valid structure from supported claims.", "Un ejercicio para distinguir una estructura válida de afirmaciones respaldadas.")}</p></header><section className="case-grid"><h2>{t("The exercise", "El ejercicio")}</h2><div className="prose"><p>{teaching.scope}</p><p><strong>{t("Prompt:", "Prompt:")}</strong> {teaching.exercise.prompt}</p><p><strong>{t("Failure case:", "Caso de fallo:")}</strong> {teaching.exercise.failure}</p></div></section><section className="case-grid"><h2>{t("Review criteria", "Criterios de revisión")}</h2><div className="prose"><ol>{teaching.exercise.criteria.map(item => <li key={item}>{item}</li>)}</ol><p>{t("A useful review asks two separate questions: does the reference exist, and does it support the statement? The first can be checked deterministically; the second still requires assessing meaning.", "Una revisión útil plantea dos preguntas: ¿existe la referencia y respalda la afirmación? La primera admite una comprobación determinista; la segunda requiere evaluar el significado.")}</p></div></section><section className="case-grid"><h2>{t("What this demonstrates", "Qué demuestra")}</h2><div className="prose"><p>{t("Define failure behavior before asking an agent to execute. A missing source should remain visible as missing evidence, so the next step is a review or a better input.", "Define el comportamiento ante fallos antes de ejecutar un agente. Una fuente ausente debe seguir visible como evidencia faltante, para que el siguiente paso sea revisar o mejorar la entrada.")}</p><p>{teaching.exercise.limits}</p><p><strong>{t("Try the original fixture:", "Prueba el ejemplo original:")}</strong> {t("download the example and run", "descarga el ejemplo y ejecuta")} <code>{teaching.exercise.command}</code>.</p><p>{teaching.exercise.result}</p><a className="text-link" href={teaching.exercise.download} download>{t("Download the runnable exercise \u2193", "Descargar el ejercicio ejecutable ↓")}</a><br /><Link className="text-link" href={localePath(locale, "/work/evalgate")}>{t("See executable acceptance checks in Evalgate \u2197", "Ver comprobaciones ejecutables en Evalgate ↗")}</Link></div></section></main>;
}

import { pageMetadata } from "@/lib/page-metadata";
import { portfolio } from "@/data/portfolio";
export const metadata = pageMetadata({ title: "Experience", description: "Jorge Jacome’s frontend engineering and technical leadership experience at Oracle, ESPN / Disney, American Express and earlier roles.", path: "/experience" });
export default function Experience() {
    return <main id="main" className="shell" data-content-version={portfolio.version}><header className="page-heading"><p className="eyebrow">Career / Experience</p><h1>Hands-on engineering.<br />Shared direction.</h1><p>{portfolio.identity.summary}</p><div className="actions"><a className="button" href={portfolio.links.resume} download>Download résumé ↓</a></div></header>{portfolio.roles.map(role => <section className="role" key={role.id} aria-labelledby={`role-${role.id}`}><div><h2 id={`role-${role.id}`}>{role.company}</h2></div><div>{role.positions.map(position => <div className="position" key={position.title}><h3>{position.title}</h3><p className="period">{position.period}</p><ul>{position.highlights.map(item => <li key={item}>{item}</li>)}</ul><div className="tag-list">{position.technologies.map(tech => <span key={tech}>{tech}</span>)}</div></div>)}{role.id === "amex" ? <p className="evidence">Component, mentoring and performance figures are historical résumé claims for American Express. The load-time measurement has not been reproduced here.</p> : null}</div></section>)}<section className="role"><h2>Education</h2><div><h3>{portfolio.education.degree}</h3><p className="period">{portfolio.education.institution} · {portfolio.education.period}</p></div></section><section className="role"><h2>Recognition</h2><div>{portfolio.awards.map(award => <div key={award.title}><h3>{award.title}</h3><p className="period">{award.organization} · {award.date}</p><p>{award.description}</p></div>)}</div></section><section className="role"><h2>Languages</h2><p>{portfolio.languages.join(" · ")}</p></section><section className="contact"><div><h2>Let’s build something useful.</h2><a className="text-link" href={portfolio.links.email}>Contact Jorge ↗</a></div></section></main>;
}

import { pageMetadata } from "@/lib/page-metadata";
import Link from "next/link";
import { portfolio } from "@/data/portfolio";
export const metadata = pageMetadata({ title: "Evidence before completion", description: "An original public teaching exercise on structured AI responses, source validation and human review.", path: "/notes/evidence-before-completion" });
export default function Teaching() {
    const { teaching } = portfolio;
    return <main id="main" className="shell" data-content-version={portfolio.version}><header className="page-heading"><Link className="text-link" href="/">← Back to portfolio</Link><p className="eyebrow" style={{ marginTop: 30 }}>Engineering note / Public teaching example</p><h1>{teaching.exercise.title}</h1><p>A small exercise in separating valid structure from supported claims.</p></header><section className="case-grid"><h2>The exercise</h2><div className="prose"><p>{teaching.scope}</p><p><strong>Prompt:</strong> {teaching.exercise.prompt}</p><p><strong>Failure case:</strong> {teaching.exercise.failure}</p></div></section><section className="case-grid"><h2>Review criteria</h2><div className="prose"><ol>{teaching.exercise.criteria.map(item => <li key={item}>{item}</li>)}</ol><p>A useful review asks two separate questions: does the reference exist, and does it support the statement? The first can be checked deterministically; the second still requires assessing meaning.</p></div></section><section className="case-grid"><h2>What this demonstrates</h2><div className="prose"><p>Define failure behavior before asking an agent to execute. A missing source should remain visible as missing evidence, so the next step is a review or a better input.</p><p>{teaching.exercise.limits}</p><p><strong>Try the original fixture:</strong> download the example and run <code>{teaching.exercise.command}</code>.</p><p>{teaching.exercise.result}</p><a className="text-link" href={teaching.exercise.download} download>Download the runnable exercise ↓</a><br /><Link className="text-link" href="/work/evalgate">See executable acceptance checks in Evalgate ↗</Link></div></section></main>;
}
